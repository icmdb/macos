#!/bin/bash
#

# ssh-keygen -t rsa -C "your.email@example.com" -b 4096

[ -d "${HOME}/Apps" ] || mkdir -p ${HOME}/Apps/

# install brew
# @TODO
brew install telnet
brew install tree
brew install wget

# install java, go, python, node

# install maven

# install kubectl helm

# install terraform
